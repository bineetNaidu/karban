query GetProjectById($id: ID!) {
  getProjectById(id: $id) {
    _id
    projectName
    projectDescription
    tabs {
      _id
      tabName
      cards {
        cardId
        cardBody
      }
    }
  }
}
